//for testing the html. output into snapshot file. every time the html changed the test will fail. hit w(watch) and the u - to updated the snapshot file.

import React from "react";

const Basic = () => {
  return (
    <div>
      <h1> Basic Test</h1>
    </div>
  );
};

export default Basic;
